# Copy this file to ".env.local" and fill in your keys.
# ".env.local" is gitignored — never commit real values.
#
# Get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# ---- Supabase (Phase B) ------------------------------------------------------
# These are injected automatically once the `supabase-citron-school` resource
# is connected to this Vercel project (then `vercel env pull .env.local`).
# The app talks to Supabase over HTTPS via these — NOT via POSTGRES_URL (which
# on this project belongs to the separate Neon integration).
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key   # server-only; never exposed to the browser
# Direct Postgres connection string for migrations only (psql). Prefer the
# Supabase "non-pooling" URL. Guarded to a supabase host by scripts/apply-migrations.mjs.
SUPABASE_DB_URL=postgresql://postgres:PASSWORD@db.YOUR-PROJECT.supabase.co:5432/postgres

# ---- LinkedIn read-only sync (Phase F) — DORMANT until you set these ----------
# The scheduled sync + backfill mirror the owner's auto-posted LinkedIn posts
# (produced by the SEPARATE field-guide-builder app) into Supabase -> /blog.
# EVERYTHING here is inert until FGB_READONLY_DATABASE_URL is set. See
# RUNBOOK-LINKEDIN.md for the exact enable steps (read-only role + contract view).
#
# Shared secret guarding /api/cron/linkedin-sync. Vercel auto-sends it as
# `Authorization: Bearer <CRON_SECRET>` to cron runs. Generate: `openssl rand -hex 32`.
CRON_SECRET=generate-a-long-random-string
# LEAST-PRIVILEGE, READ-ONLY connection string to field-guide-builder's Neon DB.
# Points at a SELECT-only role that can read ONLY the contract view. Keep sslmode=require.
# Leave UNSET to keep the sync dormant.
FGB_READONLY_DATABASE_URL=postgresql://portfolio_readonly:PASSWORD@ep-xxxx.us-east-1.aws.neon.tech/neondb?sslmode=require
# Optional overrides (sensible defaults if unset):
# FGB_READONLY_VIEW=public.linkedin_posts_view   # the contract view name in FGB
# FGB_SYNC_WINDOW_DAYS=7                          # overlapping look-back window

# ---- Admin auth (Phase B) — GitHub OAuth, single user ------------------------
# Register a GitHub OAuth App; callback URL must be <deployment-origin>/api/auth/callback.
GITHUB_OAUTH_CLIENT_ID=your-oauth-app-client-id
GITHUB_OAUTH_CLIENT_SECRET=your-oauth-app-client-secret
ADMIN_GITHUB_ID=1234567          # your NUMERIC GitHub user id (not username): https://api.github.com/users/<login>
SESSION_SECRET=generate-a-long-random-string   # e.g. `openssl rand -hex 32`
